# Documents

Drop files here, then run from the project root:

```bash
python -m dreamrag ingest
```

Supported formats: `.txt`, `.md`, `.rst`, `.csv`, `.json`, `.yaml`, `.toml`, `.pdf`, `.docx`, `.doc`
